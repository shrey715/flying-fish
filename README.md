# How to run?

### 1. Set up environment variables:

```bash
export GOOGLE_API_KEY="your_google_api_key_here"
``` 
### 2. Download the Kaggle dataset and place it in the `data/` directory. (like /data/*.csv)

### 3. Run Docker container:

```bash
docker compose up
```